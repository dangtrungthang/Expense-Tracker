import React from 'react';
import { StyleSheet } from 'react-native';
export const stylesPopupAccount = StyleSheet.create({
    container:{
        justifyContent:'center',
        alignItems:'center',
        flexDirection:'column',
        flex:1
    },
    header:{
        height:40,
        marginBottom:20,
        justifyContent:'center',
        alignItems:'center',
        borderBottomWidth:1,
    },title:{
        fontWeight:'bold',
    }
});